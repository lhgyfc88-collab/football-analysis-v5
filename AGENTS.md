# AGENTS.md

本文件约束在本仓库中工作的 Codex、OpenClaw 与其他自动化代理。

## 项目目标

将本项目建设为一个可验证、可复盘的足球数据分析与内容生产系统。网站、自媒体内容和自动化任务必须基于真实数据，不得把演示数据、默认值或启发式规则描述成已核实事实。

## 数据真实性红线

1. 不得虚构伤停、首发、战意、赛程密度、盘口、赔率、凯利指数、近期状态或历史命中率。
2. 数据缺失时必须明确显示“数据不足”“尚未核实”或同等含义，不得自动补成肯定结论。
3. `demo` 数据只能用于界面演示；`api_basic` 数据只能表示基础赛程，不得用于生成具体投注建议。
4. 每个公开概率、结论或图表都应能追溯到数据来源、抓取时间和模型版本。
5. 自媒体内容不得使用“稳赚”“稳赢”“内幕”“保证命中”等表述，也不得伪造用户收益、战绩或评价。
6. 默认只生成待审核草稿；除非用户明确批准且使用平台授权接口，否则不得自动发布。

## 代码工作流

1. 先阅读 `README.md`、相关模块和现有测试，再修改代码。
2. 不直接修改或推送 `main`；使用独立分支并通过 Pull Request 提交。
3. 修改后至少运行：
   - `python -m compileall football_predictor`
   - Flask 根路由测试，确认返回 HTTP 200
   - 与改动相关的单元测试或最小回归检查
4. 不把 API Key、Cookie、Token、OAuth 凭据或个人信息提交到仓库。
5. 失败时报告根因、已执行的检查和未完成项，不得把未验证结果描述为成功。

## 自媒体内容规则

公开内容优先采用以下类型：

- 数据看点与概率解释
- 模型方法和指标科普
- 赛后复盘、误差分析与校准结果
- 网站开发过程、功能更新和公开实验

每份内容应包含：数据日期、数据来源状态、明确的不确定性说明，以及指向网站对应页面的行动引导。若数据源为演示数据或仅有基础赛程，应改为科普或产品演示内容，不得输出比赛方向和比分推荐。

## 自动化权限

- OpenClaw 负责调度、通知、草稿整理和任务编排。
- Codex 负责仓库分析、代码修改、测试和 Pull Request。
- 使用 `tools.exec.mode: "auto"` 或更严格权限；不得默认启用 `yolo`、`danger-full-access` 或无审查的生产操作。
- 部署、数据库迁移、付费配置、公开发布和删除数据均需明确人工确认。
